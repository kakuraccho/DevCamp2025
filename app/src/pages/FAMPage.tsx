import { Link } from "react-router-dom"

export default function FAMPage() {

    return (
        <div>
            this is tech a page.
            <Link to="/">dashboard</Link>
        </div>
    )
}